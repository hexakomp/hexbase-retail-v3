# Docker environment — development only
# Copy to .env.docker and fill secrets before use

APP_KEY=base64:CHANGE_ME_32_CHARS_BASE64_STRING==

DB_ROOT_PASSWORD=secret
TENANT_CODE=demo

# Passed into backend container
DB_HOST=mysql
DB_PORT=3306
DB_DATABASE=hexbase_central
DB_USERNAME=root
DB_PASSWORD=secret
